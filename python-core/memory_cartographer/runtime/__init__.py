"""
memory_cartographer.runtime
============================

Subprocess entry point and runtime support for the memory_cartographer Python core.

The ``entrypoint`` module is the canonical subprocess entry point invoked by the
TypeScript adapter:

    python -m memory_cartographer.runtime.entrypoint

See: docs/architecture/memory-cartographer/runtime-boundary.md
"""
