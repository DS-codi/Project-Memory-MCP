[package]
name = "pm-cli"
version = "0.1.0"
edition = "2021"
description = "Project Memory MCP — interactive CLI launcher"

[[bin]]
name = "pm-cli"
path = "src/main.rs"

[dependencies]
crossterm = { version = "0.27", features = ["event-stream"] }
ratatui = "0.26"
