/**
 * Chat Module — Stripped (Plan 01: Extension Strip-Down)
 *
 * All chat/bridge/tool exports have been archived to src/_archive/chat/.
 * This barrel is intentionally empty. It remains so that stale imports
 * produce a clear compile error rather than a missing-module error.
 */
